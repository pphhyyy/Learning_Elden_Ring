using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class version_0 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("最初的版本");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
